---
description: 上下文压缩与 prompt 历史记录规则
alwaysApply: true
---

# 上下文压缩与 Prompt 历史 (Context Compression & Prompt History)

## 触发条件

在以下任一情况**自动执行**或**主动建议**将 prompt 与回复总结追加到 `prompt_history.md`：

1. **用户明确请求**：用户说「上下文压缩」「阶段性总结」「请总结并追加到 prompt_history」等
2. **自然断点**：完成一个多步骤任务（如实现新功能、修复多个 Bug）后，对话轮次较多时
3. **用户提及 context 满**：用户说「context 快满了」「90% 了」「需要压缩」等

## 执行动作

1. 读取 `prompt_history.md` 当前内容，确认最新编号
2. 将**本对话中尚未记录**的 prompt 与对应回复总结，按以下格式追加：

```markdown
---

## N. [简短标题]

**Prompt 要点**：[用户核心诉求，1-3 句]

**回复总结**：[实现/修改要点，分条列出]
```

3. 保存文件

## 格式规范

- 编号递增
- Prompt 要点：提炼用户意图，不复制全文
- 回复总结：列出修改的文件、新增/变更的逻辑、关键结果

## 文件位置

`prompt_history.md` 位于项目根目录。
