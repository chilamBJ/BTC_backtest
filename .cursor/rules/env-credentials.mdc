---
description: 从 .env 读取密码与配置执行命令
alwaysApply: true
---

# 凭据与配置管理

## 原则

- 用户提供的密码、API Key、Token 等敏感信息，**只写入 `.env`**，不提交到 git
- 执行需要密码或用户输入的命令时，**优先从 `.env` 查找**相关配置

## 本项目的 .env 配置项

| 配置项 | 用途 |
|--------|------|
| `DEPLOY_PASSWORD` | SSH 部署/远程执行密码 |
| `SYNC_ECS_SERVER` | 云端服务器地址 |
| `SYNC_ECS_USER` | SSH 用户名 |
| `TELEGRAM_BOT_TOKEN` | Telegram 告警 |
| `ADMIN_CHAT_ID` | Telegram 管理群组 |

## 执行远程命令

使用 `scripts/ssh_remote.sh` 从 .env 读取密码执行 SSH：

```bash
bash scripts/ssh_remote.sh "command to run"
```

示例：
- `bash scripts/ssh_remote.sh "tail -20 /root/btc_collector/collector.log"`
- `bash scripts/ssh_remote.sh "cd /root/btc_collector && python3 scripts/check_pm_api.py"`

## 部署

`scripts/deploy_to_ecs.sh` 会自动从 .env 读取 `DEPLOY_PASSWORD`、`TELEGRAM_BOT_TOKEN` 等，无需交互输入。
